#menggunakan cara biasa (output = bilangan akan membentuk baris dan kolom)
import numpy as np
arr = np.array([4, 3, 2, 1])
print(arr[1])

#menggunakan perulangan (output = bilangan akan berjejer kebawah)
import numpy as np
arr = np.array([4, 3, 2, 1])
for x in arr:
    print(x)
